package nrg.inc.bykerz.reports.infrastructure.persistence.jpa.repositories;

import org.springframework.stereotype.Repository;

@Repository
public class ReportRepository {
    // Placeholder repository for reports bounded context.
    // Add persistence methods here when report persistence/queries are required.

    /**
     * Placeholder helper: in future this can be used to persist or fetch aggregated report snapshots.
     * Kept minimal for now to avoid breaking compilation when integrating later.
     */
    public void noop() {
        // no-op for placeholder repository
    }
}
